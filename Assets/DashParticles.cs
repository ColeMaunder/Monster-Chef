using UnityEngine;
using System.Collections.Generic;
public class DashParticles : MonoBehaviour
{/*
    public ParticleSystem ps;
    public ParticleSystemForceField psff;
    PlayerData player;
    void Start()
    {
        player = GameObject.FindWithTag("PlayerData").GetComponent<PlayerData>();
    }

    void Update()
    {
        if(player.GetCooldownTimer() > 0f){
            player.DeIncCooldownTimer();
            
            if(player.GetCooldownTimer() <= 1f){
                psff.enabled = true;
            }else{
                ps.Play();
            }
        }else{
            ps.Stop();
            psff.enabled = false;
        }
    }*/
}
